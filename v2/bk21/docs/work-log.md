# BK21 작업 목록

2026-09-01 ~ 09-02

---

## 1. 자리 만들기

- `biblo.ai/bk21` 신설
- `robots.txt` 색인 차단 (`/bk21`)
- `server.js` 관문에 `/bk21` 추가
- `bk21-board` 대시보드를 `biblo.ai/bk21/board/` 로 이관
- `_src/` 에 Vercel 원본(middleware·login·api·vercel.json) 동봉
- `.md` MIME 타입 추가

## 2. 문서

- `bk21-board.md` — 운영 기록 (폴더·Vercel ID·인증 구조·환경변수·작업 내역·미해결)
- `score-standard.md` — 산출 기준 (SGCPI+ 채택)
- `ai-paper-rule.md` — AI 논문 추출 규칙
- `ai-paper-rule.py` — 규칙 실행본
- `work-plan.md` — 보드 개편 작업계획서
- `screen-design.md` — 화면 설계서
- `sgcpi-indicator-map.md` — SGCPI+ 지표 지도
- `work-log.md` — 이 문서
- `bk21/index.html` 인덱스에 문서 링크 8개

## 3. 접속 정보

- `~/Documents/biblo-운영/보안/BIBLO_접속정보.md` 작성 (깃 밖 · 700/600)
- Supabase 3계정 · Vercel 12프로젝트 · EC2 배포 경로 · API 키 6종 · EBSCO · 교내 DB 3종
- 깃에 평문 노출된 키 3건 기록 (교체는 대표 판단으로 보류)

## 4. 산출 기준 확정

- SGCPI+ 배점표를 기준문서로 채택
- 저널등급 g — 계열별 표 (인문사회 6/3/2 · 이공의약 6/5/4/3)
- 역할가중 w — 주저자 1 · 공저자 1/2, 단독저자를 주저자로
- 계열 판정 — 경영자는 계열 · 상세는 학과 · 개인창은 개인 소속
- JCR 상위 5% 산출 가능 판정 (앞선 «불가» 정정)
- 자체 기준 4항 — JCR 50% 밖 2점 · 이공의약 KCI 2점 · 기타 국제논문 3점 · 미판정 0점
- 학회발표를 매뉴얼 값으로 교체 (국제 2 · 국내 1, 주저자만)
- 기간 단위 연도 유지 결정
- AI 논문 규칙 — 1군 38어 · 2군 문맥조건 · 회색 108건 사람 확인

## 5. 데이터 산출

- `paper_citation_metric` 의 `IF_PUB_RANK` 를 JCR 백분위로 환산
- ISSN 전파로 등급 회수 — 미판정 42.3% → 5.5%
- 저널 4,216종 대표 백분위 산출
- AI 논문 판정 실행 — 자동 1,775 · 회색 107
- 학술대회 국제/국내 구분 — 국제 3,324 · 국내 0 (원천 미색인)
- RQ 세부지표 분해 — 학술지 · 학회 · AI 논문
- IQ 해외공동연구 · SQ 산업계 협업 — 저자 소속으로 판정
- 인물→학과 매핑 `kids` 재귀 (미매핑 26.1% → 0)
- 논문 6필드 · 교수 6필드 · 학과 6필드 신설
- 계열 집계 키 `gyo` 신설
- `bk21_tree.json` 재생성

## 6. 화면 — exec (KPI)

- 상단 배지 교체 — 「잠정 v0.1」 → 「SGCPI+ 배점 · 참여교수 적용」
- SGCPI+ 환산점수 카드 — 계열 3장 · 등급 띠 · 미판정 편수
- 계열 카드 → 학과 펼침 → tree 이동
- 역량 레이더 4영역 (RQ·IQ·SQ 점등 · LQ 미수집)
- 영역별 분해표 29지표 전수
- 미수집 영역 카드 4장 (배점·입력주체 표기)
- AI 역량 단면 (신설 9개 중 6개 · 학과별 막대 · AI관련학과 배지)
- JCR 4구간 분포

## 7. 화면 — tree (성과관리)

- 개인창 지표 박스 4 → 6 (환산점수 · 환산편수)
- 소속에 연구그룹 표기
- 등급 구성 띠
- 논문별 산출 내역 표 (판정근거 병기 · 미판정 노출 · 30행 페이징)
- AI 논문 배지

## 8. 화면 — pool (데이터풀)

- 「산출에서 빠진 것」 카드
- 학과별 미판정 표

## 9. 화면 — sheet (대학별 성과표)

- SGCPI+ 환산점수 열 (타 대학은 「산출 불가」)

## 10. 화면 — ucmp (대학 BK21 비교) · 신규

- 좌측 레일 9번째 메뉴 추가
- KPI 6장
- 연도별 논문 표 (2020~2025 · 증감률)
- 분야별 히트맵 (8분야 × 8대학)
- 질 지표 5항목 순위
- BK21 참여 규모 표
- 데이터 근거 카드

## 11. 인사이트

- `.insight` 컴포넌트 (일반 · 경고)
- exec 11블록 · univ 8 · net 7 · glob 9 · sheet 5 · pool 5 · 분야탐색 3 · 개인창 2
- 데이터 기반 카드 4종에 `ins` 필드 신설 (pool 노드 6 · 레이어 2)
- 카드 밖 인사이트 3장을 카드 안으로 이동

## 12. 연구자 팝업

- 개인성과 11개 메뉴 구조
- BK21 참여/미참여 배지
- 3단 레이아웃 (간략정보 · 목록 · 상세)
- 슬라이드 16 원본 이식 (CSS 13.2KB · JS 29KB · 차트 11개)
- IIFE 격리 (`window.RP`)
- CSS `.rpwrap` 스코프
- 클래스 충돌 8개 접두사 격리
- 어댑터 `toPerfShape()` — 우리 데이터 → 원본 P 형태
- 고정 무대 1718×812 배율 + 쌓기 전환
- 빈 상태 설계 (미참여자 카드·지표·대표작 채움)
- 미참여자 11탭 전부 OpenAlex 로 채움
- 외부 조회 차단 시 보드 보유값 폴백 + 안내
- 근거 논문 4편
- 간격 균일화 (사방 18 · 칸 사이 18 · 세로 22)
- 바깥 검정 제거 · 카드 불투명 전환

## 13. 라우팅 · 버그

- `setView` 목록에 `ucmp` 누락 수정 (화면이 안 보이던 원인)
- 해시 딥링크 `routeHash()` 신설 (`#field=` · `#뷰이름`)
- 팝업 세로 스크롤 (`max-height:92vh` · `overflow-y:auto`)
- 모달 폭 특정도 수정 (`.modal .prbox.prwide`)
- 메뉴 검색줄 한 줄 고정 · `.mlist` 스크롤 안전장치
- CSS 주석 `*/` 조기종료 수정
- `.gyrow` 특정도 · 기존 반응형에 위임
- HHI 스케일 정합 (836,000 → 84)
- `explore` 가 `with_` 를 팝업에 전달하도록 수정
- 오버라이드 위치를 이식 CSS 뒤로 이동

## 14. 타 대학 자료

- `BK4 학교 별` 10파일 정제 — 8개 대학 4,226명
- 연세대·한양대 성명 회수 (마지막 열 덩어리 파싱)
- 학과 175종 → 표준분야 25종 매핑 (미분류 5.0%)
- 이름 마스킹 (성 1자 + `*`)
- `bk4.json` 생성

## 15. DB

- `paper` 스키마 발견 — `oa_work` 1.08억 · `oa_work_author` 3.26억
- `bk21.univ_work` 생성 (8개 대학 · 90만 행 · 인덱스 3)
- `ucmp.json` 생성 (연도 · 분야 · 질 지표 · 참여 규모)
- `build.sh` 에 `UCMP` · `ucmp.js` 등록
- `vis.js` VIEWS 에 `ucmp` 등록

## 16. QA

- Playwright 화면 검증 도구 33종
- 9개 화면 실제 크기 검증 (DOM 존재 → 보이는 크기로 기준 변경)
- 팝업 11탭 × 참여자/미참여자 전수
- 반응형 1900/1500/1240/900/700px
- 다크 테마
- OpenAlex 차단 상태 재현 검증
- 콘솔 오류 감시

## 17. 정정한 것

- 「논문 1억건 없음」 → `paper` 스키마에 있었음
- 「PP(top 10%) 없음」 → 학과 단위로 산출돼 있었음
- 「계열 집계 없음」 → `kpi.json` 에 `gy` 키 존재
- 「JCR 상위 5% 원천 없음」 → `IF_PUB_RANK` 로 산출 가능
- 「보드에 점수 있음」 → 「실적순」은 논문 편수 정렬
- 「미분류 42% 는 품질 문제」 → 조인 실패
- 「SGCPI+ 표에 학회발표 자리 없음」 → 있었음
- 「지표 25개」 → 29개
- 「OpenAlex 무료 무제한」 → 일일 예산제
- 개인 이름 매칭 100% 매칭률 → 오귀속 (폐기)

## 18. 소스 이관 (09-02)

- 빌드 조각 16 · 데이터 14 를 `v2/bk21/board/_src/` 로 이관
- 데이터 산출 스크립트 30 을 세션 임시폴더에서 건짐 (`gen/` · `gen/bk4/`)
- 팝업 이식 스크립트 3 (`port/`)
- QA 스크립트 45 (`qa/`)
- 스크립트 31개에 하는 일 한 줄 주석 추가
- `build.sh` 새 위치 기준으로 재작성 · 인증 패치 내장 · `--vercel` 사본
- 옛 Vercel 부속을 `vercel-legacy/` 로 분리
- `data/` 를 `.gitignore` 로 제외 (공개 저장소 · 16MB)
- 바이트 동일 검증 (16,997,835 B)
- 문서 — `source-map.md` · aws 쪽 `README.md` 2 + `SOURCE-MAP.md`
- **저장소가 공개임을 확인** — 보드 17MB 가 로그인 없이 받아짐
- 배포 워크플로가 서버에서 보드를 보관·복원하도록 변경
- 보드를 깃에서 빼고 `.gitignore` 등록
- 이력 재작성 — 보드 18개 판본 287MB 를 전 커밋에서 제거 (`.git` 253MB → 149MB)
- 저장소 통째 백업 (153MB 번들)
- Actions 로그로 보관·복원 검증

## 19. 미완

- LQ 교육역량 8지표 · 나머지 미수집 16지표 (학사 시스템 원천)
- 보드에 `openalex_author_id` 심기 (팝업 API 제거)
- 타 대학 참여자 단위 성과 (이름 매칭 미해결)
- `tr=U` 잔존 5.5% 정밀화
- 학기 단위 집계
- AI 회색 107건 사람 확인
- Vercel `bk21-board.vercel.app` 미반영
- 14MB 단일 파일 구조 (갱신마다 깃 블롭 증가)
- **보드를 서버에 올릴 통로 없음 — EC2 키 확보 필요** (지금은 화면 수정이 서버에 반영 안 됨)
- 옛 커밋 번호로는 보드가 아직 받아짐 (깃허브 자체 청소 대기 · 2026-09-02 대표 판단으로 둠)
